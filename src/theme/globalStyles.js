import { css } from "@emotion/react";

const GlobalStyles = css`
  body, #root {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    min-width: 100vh;
    margin: 0;
  }
`;

export default GlobalStyles;
